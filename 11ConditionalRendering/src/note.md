There are four ways of condional rendering

1. using if else statements
    - can not be written in the JSX rather in the render or outside the jsx return
2. Element variables
3. Ternary condtional operators
4. short circuit operators